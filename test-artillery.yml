config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Content-Type: "application/json"
scenarios:
  - name: "mix GET + POST"
    flow:
      - get:
          url: "/free"
      - post:
          url: "/echo"
          json:
            test: "artillery"
